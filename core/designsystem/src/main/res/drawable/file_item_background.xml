<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/white" /> <!-- 기본 투명 배경 -->
    <corners android:radius="6dp" />
    <stroke
        android:width="0.8dp"
        android:color="@color/grey_50" />
</shape>
